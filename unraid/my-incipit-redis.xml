<?xml version="1.0"?>
<Container version="2">
  <Name>incipit-redis</Name>
  <Repository>redis:7-alpine</Repository>
  <Registry>https://hub.docker.com/_/redis</Registry>
  <Network>incipit</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/Healzangels/incipit-api/issues</Support>
  <Project>https://github.com/Healzangels/incipit-api</Project>
  <Overview>
    Redis for incipit-api. Caches provider search results (which is what keeps the
    stack inside Hardcover's 60 req/min limit) and audnexus responses, and it is
    where alternate cover art is stored between a search and a later item lookup.

    OPTIONAL but strongly recommended: without REDIS_URL the API still runs, but
    every book lookup that needs alternate covers would re-run a full provider
    search on every request, so that feature disables itself instead.

    Publishes NO port: only incipit-api talks to it over the "incipit" network.
    Start this BEFORE incipit-api.
  </Overview>
  <Category>Tools:Utilities Network:Other</Category>
  <WebUI/>
  <TemplateURL/>
  <Icon>https://raw.githubusercontent.com/Healzangels/incipit-api/nightly/assets/incipit-icon.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires>A user-defined docker network named "incipit" (docker network create incipit)</Requires>
  <Config
    Name="Cache storage"
    Target="/data"
    Default="/mnt/user/appdata/incipit-redis"
    Mode="rw"
    Description="Redis append-only/snapshot directory. Losing it costs only cache warmth, never data."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false">/mnt/user/appdata/incipit-redis</Config>
</Container>
